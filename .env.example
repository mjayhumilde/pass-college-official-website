# npm run build and npm run start:prod override this with production.
VITE_APP_ENV=development

# API and Socket.IO server origin; do not append /api/v1.
VITE_API_URL_DEV=http://localhost:2000

# Replace with your deployed backend origin before building for production.
VITE_API_URL_PROD=https://api.example.com
